# 첫 성능 개선 결과

2026-09-17 · 비교 기준 `b36afb7` · macOS arm64 / Python 3.9.6

Python 엔진의 강제승 오판, 시간 제한, 금수 계산을 수정했다. 기존 앱 호환 규칙(기본 양색 금수)은 유지했다. Rapfi 바이너리/래퍼와 Cython 빌드는 이번 변경에 포함하지 않았다.

## 변경 내용

- **필수 방어 우선:** `search_move`는 내 즉승 다음에 상대 즉승을 확인한다. 기존 반례에서 잘못된 `(7, 6)` 공격 대신 `(10, 7)`을 막는다. VCF/VCT도 상대가 먼저 승리할 수 있으면 이중 위협을 필승으로 반환하지 않는다.
- **증명과 중단 구분:** 새 `prove_win_move_ex`는 `PROVEN_WIN`, `NO_PROOF`, `TIMEOUT`을 구분한다. 기존 `prove_win_move`의 move/None 반환 형태는 유지했다. 상대 VCF의 탐색 실패나 timeout을 “방어 성공”으로 해석하던 경로를 제거했다.
- **공통 시간 제한:** `perf_counter` 기반 절대 deadline을 후보 정렬·평가·증명·재귀 탐색에 전달한다. 만료 후 정적 평가를 새로 실행하지 않는다. 중단한 반복심화 결과를 완료 깊이로 표시하지 않고, 마지막 완료 결과나 합법 후보로 돌아간다.
- **보드 복원:** 임시 착수는 원래 값을 저장하고 `finally`에서 복구한다. 공격수와 강제 방어수가 동시에 놓인 상태에서 중단되어도 입력을 보존한다.
- **패턴 계산 절감:** 승리/장목 검사는 라인 리스트를 만들지 않고 직접 연속 돌을 센다. 열린삼은 가상 착수마다 보드를 반복 조회하는 대신 한 번 읽은 라인에서 검사하고, 최대 8,192개 패턴을 캐시한다. 캐시 키는 보드 객체가 아닌 색 상대적인 라인 내용이다.
- **보수적인 증명:** 비강제 위협의 방어 응수는 주변 후보만이 아니라 모든 합법 빈칸을 검사한다. 일반 탐색도 단일 필수 방어가 탐색 폭 제한에 밀려 사라지지 않게 했다.
- **문서/자체 검증 정정:** 백 장목의 기본 설정과 자체 검증의 모순, 항상 참인 assertion을 수정했다. README의 규칙·통합 동작·증명 한계를 실제 구현에 맞췄다.

## 측정 결과

기존과 동일한 합성 국면 3개에서 각 3회 측정한 중앙값이다. [이전](benchmarks/baseline.json) / [이후](benchmarks/after.json). 세 국면의 `best_move` 반환값은 점수·추천수·설명까지 동일했다.

| 국면 | `best_move` 이전 → 이후 | 속도 비율 | 50ms 예산 `search_move` 이전 → 이후 |
|---|---:|---:|---:|
| 초반 2돌 | 11.7 → 3.0ms | 3.88배 | 79.1 → 50.0ms |
| 12돌 | 37.9 → 10.7ms | 3.54배 | 119.8 → 50.0ms |
| 32돌 | 48.6 → 15.5ms | 3.14배 | 161.3 → 50.0ms |

12돌 `prove_win_move`의 50ms 예산 중앙값도 128.7ms에서 50.0ms로 줄었다.

추가로 2/12/32/60/100돌을 포함한 **100개 합법·비종료 합성 국면 × 5회 × 2 API = 1,000회**를 측정했다. 예산은 모두 50ms다. 원시 시간 표본과 환경·코드 SHA-256은 [deadlines.json](benchmarks/deadlines.json)에 있다.

| API | 호출 수 | p50 | p95 | 최대 | 60ms 초과 |
|---|---:|---:|---:|---:|---:|
| `search_move` | 500 | 12.41ms | 50.065ms | 50.106ms | 0 |
| `prove_win_move_ex` | 500 | 5.48ms | 50.056ms | 58.277ms | 0 |

모든 호출에서 입력 보드가 보존되었고, 반환된 수는 빈칸이며 해당 색의 금수가 아니었다. 빠른 즉승/방어 반환이 포함되므로 p50은 검색 전체가 완료되는 데 필요한 시간이나 기력을 뜻하지 않는다. 패턴 캐시는 같은 프로세스 내 호출 사이에 유지했고, 최초 표본도 포함했다.

## 검증

- 표준 라이브러리 `unittest` **16개 통과** 및 `python3 omok_ai.py` 자체 검증 통과.
- 열린삼은 양색 각각 729개 내부 라인 패턴, 무작위 보드 1,500개 × 4방향을 기존 가상 착수 방식과 대조했다. 직접 연속 돌 계산도 기존 라인 계산과 대조했다.
- 8가지 금수 설정 조합 × 30개 무작위 보드 × 양색으로 금수 결과를 대조했다.
- 흑/백 필수 방어, 실제 즉승/VCF 승리, 거짓 VCF/VCT, 시간 초과와 증명 부재 구분, 미완료 깊이 보존, 중첩된 강제 방어 복원, 예외 복원, 이미 놓인 돌의 복원, 먼 방어점 포함을 검증했다.
- 확장 벤치마크 1,000회에서 시간·보드·합법성을 추가 확인했다.

재현:

```sh
python3 -m unittest discover -s tests -v
python3 omok_ai.py
python3 benchmarks/baseline.py --output benchmarks/after.json
python3 benchmarks/deadlines.py --output benchmarks/deadlines.json
```

## 결과 해석과 남은 작업

이번 개선은 **계산 비용 절감과 응답시간 제어, 알려진 오판 수정**이다. 동일 시간 대국이나 전술 문제집 전체를 검증하지 않았으므로 승률/Elo 향상을 주장하지 않는다.

500ms 예산의 12/32돌 `search_move`는 이제 대부분 예산 끝까지 탐색한다. 이전 125/177ms 조기 반환과 비교해 이 호출들이 더 빨라진 것은 아니다. 근거가 약한 방어/필승 조기 반환을 제거하고 더 넓은 방어 검사를 수행한 영향이 있다. 증명 가능한 수를 제한시간 안에 찾는 비율과 기력은 후속 검증 대상이다.

시간 제한은 협력적 검사이며 OS 실시간 보장이 아니다. 0/음수 예산 또는 합법 후보 확보 이전에 예산이 끝나면 `search_move`는 None을 반환한다. 시간 예산을 넘기는 큰 연산을 줄였지만 검사 사이의 작은 연산이나 OS 스케줄링 지연은 남는다.

공식 렌주의 재귀적 거짓삼은 기존 근사 판정을 유지한다. 규칙 설정은 여전히 전역이므로 탐색 중에 변경하면 안 된다. 향후 요청별 불변 규칙, 전이표/증분 평가, 실제 기보·대국 기반 기력 검증을 진행할 수 있다. Rapfi의 플랫폼 호환성과 blocking I/O 문제는 이번 범위 밖이며 남아 있다. Cython 빌드 및 해당 빌드에서의 속도/정확성은 이번에 검증하지 않았다.
