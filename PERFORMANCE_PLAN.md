# omok-ai 성능 개선 계획

작성: 2026-09-17 · 기준 커밋: `b36afb7` · 저장소: `needitem/omok-ai`

이 문서는 최초 분석 시점의 계획이다. 이후 수행한 첫 개선의 변경 내용과 검증 결과는 [PERFORMANCE_RESULTS.md](PERFORMANCE_RESULTS.md)에 기록했다.

목표는 **제한시간 안에 합법적이고 전술적으로 안전한 수를 반환하고, 같은 시간 안에서 더 깊게 탐색하는 것**이다. 응답시간과 기력을 함께 관리한다. 현재 단계에서는 분석·계측만 수행했으며 엔진 구현은 변경하지 않았다.

## 1. 확인한 기준선

환경: macOS/Darwin arm64, Python 3.9.6, 순수 Python 구현. 기본 `renju` 설정 그대로 측정했다. 실제 기본 설정은 흑·백 모두 33/44/장목 금지다.

고정된 합성 국면 3개에서 항목별 3회 실행한 중앙값이다. 실전 기보나 p95 측정은 아니며 기력 개선율을 의미하지 않는다. 예산은 최대 허용시간이므로 일찍 반환하는 것은 정상이다.

| 국면 | `best_move` | `search_move`, 예산 50ms | `search_move`, 예산 500ms | `prove_win_move`, 예산 50ms | `prove_win_move`, 예산 500ms |
|---|---:|---:|---:|---:|---:|
| 초반 2돌 | 11.7ms | 79.1ms | 540.9ms | 16.0ms | 16.2ms |
| 고정 seed 12돌 | 37.9ms | 119.8ms | 124.9ms | 128.7ms | 573.3ms |
| 고정 seed 32돌 | 48.6ms | 161.3ms | 177.4ms | 37.7ms | 187.8ms |

- 32돌 `search_move`는 50ms 예산의 약 **3.2배**, 12돌 `prove_win_move`는 약 **2.6배**가 걸렸다.
- 측정한 45회 호출에서는 입력 보드가 호출 전후 동일했다. 중단·예외 경로의 복원까지 검증한 결과는 아니다.
- 별도 cProfile 실행(32돌, `search_move`, 50ms 예산)은 총 286ms, 약 347만 함수 호출이었다. `is_forbidden` 누적 242ms(약 85%), `_states` 누적 207ms(약 72%), `_open_three_in_dir` 누적 173ms였다. **누적시간은 서로 포함되므로 합산하지 않는다.** 프로파일링 오버헤드와 시간 제한 때문에 일반 실행과 방문 경로도 달라질 수 있다.
- [원시 결과](benchmarks/baseline.json), [프로파일](benchmarks/profile.txt), [재현 스크립트](benchmarks/baseline.py)를 보관했다.

재현:

```sh
python3 benchmarks/baseline.py --output benchmarks/baseline.json
python3 benchmarks/baseline.py --profile > benchmarks/profile.txt
python3 omok_ai.py
```

마지막 명령은 현재 `omok_ai.py:836`의 백 장목 승리 assertion에서 실패한다. 코드 기본 규칙과 자체 검증의 기대가 다르다.

## 2. 우선 해결해야 할 문제

### P0 — 승리·방어 판정과 규칙의 일관성

근거: `omok_ai.py:444` `_vcf`, `:581` `_vct`, `:704` `search_move`.

`search_move`는 상대 즉승 차단보다 VCF를 먼저 실행한다. `_vcf`는 상대 즉승을 검사하지 않으며, `_vct`도 이중 완성점이면 상대 즉승 검사 전에 성공을 반환한다. 다음 8돌 국면은 흑·백 돌 수가 같고, 흑 차례에 실제 오판이 재현된다. 좌표는 0부터 시작한다.

```python
import omok_ai as ai
b = ai.empty_board()
for c in (3, 4, 5):
    b[7][c] = ai.BLACK
for c in (3, 4, 5, 6):
    b[10][c] = ai.WHITE
b[10][2] = ai.BLACK
m = ai.search_move(b, ai.BLACK, "renju", time_budget=0.5)
print(m)
# 관측: (7, 6), score=WIN, "강제승 발견(연속 사·VCF)"
# 그러나 백이 (10, 7)에 두면 즉시 승리한다.
```

계획:

1. 루트와 재귀 노드에서 내 즉승 → 상대 즉승/필수 방어 → 강제 위협 탐색 순서를 명확히 한다. 강제 방어 뒤에도 상대가 이미 승리했는지 확인한다.
2. 증명 탐색의 반환 상태를 구분한다. 예: `PROVEN_WIN`, `NO_PROOF`, `TIMEOUT`. 공격 후보·깊이를 제한했을 때의 `NO_PROOF`는 패배나 방어 성공의 증명이 아니다. 현재 `search_move:742`의 `_vcf(...) is None`을 “상대 강제승 저지”로 해석하는 경로를 제거한다.
3. `_vct`의 반경 2 방어 후보와 `_fork_unstoppable`의 국소 방어 후보가 모든 반박수를 포함하는지 검증한다. 완전성을 입증하기 전에는 전술 휴리스틱과 증명 결과를 구분한다. 검증용 구현은 전체 합법 응수를 열거한다.
4. 룰별 의미를 고정한다. README의 흑만 금수 설명과 현재 양색 금수 기본값이 다르다. `standard`도 Python에서는 5목 이상 승리로 취급되므로 Rapfi rule=1과의 계약을 대조한다. 앱 호환 룰과 공식 룰은 명시적인 별도 프리셋으로 제공하고 기존 호출의 의미를 보존한다.
5. 전역 `FORBID_CFG`를 요청별 불변 규칙 객체로 전달하는 방향으로 정리한다. 캐시와 동시 요청이 다른 규칙의 결과를 섞지 않게 한다.

완료 기준: 위 반례와 필수 방어·금수·장목·색 대칭·보드 경계·timeout 사례를 회귀 자료로 고정하고 통과한다. 기존 자체 검증의 항상 참인 assertion(`:829`)도 실제 기대값 검사로 바꾼다. 시간 초과를 강제승/방어 증명으로 표시하는 사례가 없어야 한다.

### P0 — 시간 제한을 실제 종료 조건으로 만들기

근거: `_negamax:393`, `_root_search:684`, `search_move:704`, `prove_win_move:630`.

- `_negamax`는 시간이 끝나도 비싼 `_evaluate`를 실행한다.
- 후보 생성·정렬·금수 검사·포크 검증 내부에 deadline 검사가 부족하다. 만료된 단계에서도 `_ordered` 등을 수행한다.
- `search_move:742`는 후보마다 새로 50ms를 부여한다. 전체 요청 예산과 분리되어 있다.
- 부분적으로 탐색한 반복심화 결과가 완료된 깊이처럼 표시될 수 있다.

계획:

1. 진입 시 `time.perf_counter()`로 하나의 절대 deadline을 만들고 하위 함수 전체에 전달한다. 단계별 한도는 항상 전체 deadline 이하로 제한한다.
2. `SearchContext`에 deadline, 노드 수, 중단 상태, 마지막 완료 결과를 둔다. 후보 루프와 비싼 규칙 평가 사이에서도 시간을 확인한다.
3. 만료 시 정적 평가를 새로 하지 않고 중단 신호를 상위로 전달한다. 마지막으로 완료한 탐색 깊이의 결과 또는 미리 확보한 합법 fallback을 반환한다. 진입 검증·fallback·결과 구성 비용도 총 예산에 포함한다.
4. 모든 임시 착수는 `try/finally` 또는 make/unmake 스택으로 복구한다. 중첩된 방어 착수까지 예외 안전성을 검증한다.
5. `completed_depth`, `nodes`, `elapsed_ms`, `timed_out`, `proof_status`를 계측용으로 제공한다.

완료 기준(제안): 별도 기준 장비의 100개 이상 국면, 국면당 10회 이상에서 p95 ≤ 예산 + `max(10ms, 예산의 10%)`. 최대 초과시간도 별도로 기록한다. 이는 측정 목표이며 OS 스케줄링까지 포함하는 실시간 보장은 아니다. 0/극소 예산도 안전하게 종료하고 입력 보드를 복구해야 한다.

### P1 — 금수·패턴 계산의 중복 제거

프로파일상 우선 대상은 정렬 알고리즘보다 `_states`와 열린삼/금수 판정이다.

1. 한 보드 상태·후보·색에 대해 합법성, 즉승, 사, 열린삼, 점수를 묶어 계산하고 호출 단계 사이에서 재사용한다.
2. 직선 좌표와 경계 정보를 미리 계산한다. `_states`의 문자열 리스트 생성과 `_in`/`len` 반복을 줄이고, 같은 방향 정보를 여러 검사에서 공유한다.
3. 열린삼 가상 착수마다 보드에서 다시 라인을 추출하는 대신 추출된 라인 위에서 검사한다. 현재 앱 호환 동작과 차등 검증한다.
4. 먼저 요청 내부 캐시로 효과를 측정한다. 키에는 보드 상태, 색, 룰, 금수 설정을 포함하고 크기를 제한한다. 가변 리스트 객체의 identity만으로 캐시하지 않는다.
5. 다음 단계로 후보 주변 점유 개수와 방향별 상태를 증분 갱신한다. make/unmake로 후보 추가·제거와 점수 변경을 되돌린다. 영향 범위는 실제 패턴의 참조 범위를 기준으로 정하고 전체 재계산과 대조한다.

완료 기준(제안): P0 완료 버전 대비 고정 작업량 규칙/평가 벤치마크 중앙시간 40% 이상 감소를 목표로 한다. 랜덤·경계·금수 국면에서 기존 의미를 보존하는 경로는 결과가 일치해야 한다. 증명 탐색과 후보 선택 변경으로 인한 성능·기력 효과는 별도 측정한다.

### P1 — 같은 예산에서 탐색 효율 높이기

1. Zobrist hash와 용량 제한 transposition table을 도입한다. 일반 탐색 항목은 깊이, `EXACT/LOWER/UPPER`, 점수, 최선수를 저장하고 수순·차례·규칙 설정을 키에 반영한다. timeout/미완료 결과는 exact로 저장하지 않는다.
2. 증명 탐색 캐시는 일반 점수 캐시와 구분한다. 제한된 탐색 실패를 “승리 불가능”으로 재사용하지 않는다. 엄밀한 증명 결과는 해시 충돌 검증도 고려한다.
3. PV/TT 최선수, 즉승, 강제 방어, 사 위협을 우선 정렬한다. 현재 내부 폭 6과 루트 폭 12가 강제 대응을 버리지 않도록 전술 수를 보존한다.
4. 이후 killer/history ordering과 적응형 폭을 개별 실험한다. 공격 폭 축소와 증명을 위한 방어 응수 축소는 서로 다른 정확성 조건을 갖는다.

완료 기준: 동일 노드 수에서 소요시간, 동일 시간에서 완료 깊이·전술 정답률을 각각 비교한다. 동률 결정 규칙을 고정하고, 깊이 증가만으로 기력 개선을 주장하지 않는다.

### P1 — Rapfi 실행·통신과 통합 경로

관측: 이 Mac에서 `available()`은 True지만 `_start()`는 `Exec format error`로 실패한다. 번들은 Linux aarch64 ELF다. 따라서 이번 분석에서는 Rapfi 탐색 속도나 기력을 측정하지 않았다.

근거: `rapfi_bot.py:35–120`, `:135–142`, `rapfi_engine/config.toml`.

1. 대상 OS/CPU용 바이너리를 선택하고 실행 권한·기동 handshake까지 확인한다. macOS 개발용과 Linux 배포용 검증을 분리한다.
2. `_read_until`과 `_read_result`의 blocking `readline()`을 reader thread + queue 또는 비동기 I/O로 교체한다. 무출력·부분 줄 출력 상태에서도 deadline에 반환하도록 한다.
3. lock 대기, 초기 기동, 검색, 재시도를 전체 요청 예산에 포함한다. 현재 검색 대기에는 `timeout_ms + 8초`가 쓰이며 2회 재시도하고, blocking read 자체는 그 제한도 넘길 수 있다. 종료 후 `wait()`까지 수행하고 실패 원인을 기록한다.
4. `me` 인수가 `_search`로 전달되지 않는 현재 구조를 점검한다. 차례/돌 개수/좌표/규칙의 프로토콜 계약을 실제 엔진 소스로 확인하고, 잘못된 보드나 반환 수를 검증한다.
5. 현재 프로세스 재사용은 유지하되 한 프로세스의 검색은 직렬화한다. 여러 사용자 처리 시 제한된 worker pool과 요청 취소를 설계한다. 세션 상태를 유지할 수 있을 때만 `TURN` 등 증분 전송을 검토하고, 임의 보드 요청은 전체 상태를 전달한다.
6. 현재 12 threads, 512MiB TT, DB 활성화는 Jetson 기준 설정이다. threads 1/2/4/8/12, TT 크기, DB 사용 여부를 대상 장비에서 바꿔가며 p95·처리량·메모리·기력을 비교한다. worker 수 × 엔진 threads의 과도한 경쟁을 피한다.
7. 하나의 추천 진입점으로 즉승/필수 방어 → 남은 예산 내 증명 → Rapfi → 합법성 검증/fallback을 연결한다. 각 모듈에 전체 예산을 별도로 주어 합산 지연을 만들지 않는다. 규칙이 다른 엔진 결과를 마지막에 필터링하는 것만으로 전체 수순의 적법성·기력이 보장되지는 않는다.

완료 기준: fake engine으로 무응답·부분 출력·비정상 종료·동시 요청·복구를 검증하고, 지원 플랫폼의 실제 엔진으로 흑/백·좌표·규칙 통합 검증을 통과한다. cold start와 warm search 수치를 분리한다.

### P2 — Cython/native 최적화

`build_omok.py`가 이미 있으므로 P0/P1 이후 동일 국면을 순수 Python과 Cython으로 비교한다. 무타입 Python 로직의 단순 컴파일만으로 큰 배수 향상을 가정하지 않는다. 효과가 확인된 라인 스캔·패턴 검사에 한해 타입 지정, 고정 배열 또는 작은 native 모듈을 검토한다. 경계 검사 비활성 옵션을 사용하는 만큼 모서리·인덱스·보드 복원에 대한 양쪽 구현의 일치 검증이 필요하다.

## 3. 실행 순서와 검증 체계

| 순서 | 변경 묶음 | 예상 작업량 | 통과 조건 |
|---|---|---|---|
| 1 | 규칙 프리셋/반례/측정 자료 정리, P0 오판 수정 | 1–2일 | 회귀 자료 통과, 증명 상태 구분 |
| 2 | deadline 전달, 안전한 중단·복원, 완료 깊이 보고 | 1–2일 | 지연 목표 및 보드 복원 검증 |
| 3 | 라인 재사용·요청 캐시 → 증분 평가 | 2–4일 | 결과 차등 검증 + 고정 작업량 개선 |
| 4 | TT·수 정렬·탐색 폭 실험 | 2–3일 | 시간당 전술 정답률/완료 깊이 개선 |
| 5 | Rapfi 플랫폼/통신/통합 및 자원 튜닝 | 2–3일 | 실제 엔진 통합·장애·부하 검증 |
| 6 | Cython/native 선택 적용 | 1–3일 | 유지보수 비용에 상응하는 실측 이득 |

총 9–17 작업일의 초안 추정이다. 공식 렌주 금수 판정을 새로 완전 구현하는 범위나 Rapfi 자체의 탐색 알고리즘 변경은 별도 산정한다. Mac에서 Rapfi가 주 실행 경로라면 5단계 중 바이너리 준비는 1단계 직후 진행한다.

검증 자료는 초반/중반/종반, 강제 방어, 33/44/장목, 거짓삼, 연속 사, 열린삼 연쇄, 경계, 양색 규칙, 빈/가득 찬 보드를 포함한다. 속도 비교는 같은 장비·Python·룰·스레드·DB 상태에서 실행하고, 시간 제한에 따른 경로 변화가 있는 탐색 벤치마크와 고정 작업량 마이크로벤치마크를 분리한다. 메모리 최대값, 캐시 적중률, 노드 수, p50/p95/max를 함께 기록한다.

기력은 전술 문제 정답률과 동일 시간 예산의 대국으로 확인한다. 고정 오프닝을 색 교환한 대국 쌍으로 구성하고 같은 룰을 지원하는 기준 엔진과 비교한다. 최소 200쌍을 초기 표본으로 삼되 승률의 신뢰구간을 함께 보고, 불확실하면 표본을 늘린다. 거짓 필승·불법수 회귀가 있으면 속도 향상과 무관하게 변경을 보류한다.

첫 구현 묶음은 **“상대 즉승 무시 반례 수정 + 시간 초과와 탐색 실패 구분 + deadline 준수”**를 권장한다. 이 기반을 먼저 맞춰야 이후 캐시와 깊이 확장의 효과를 믿을 수 있다.
